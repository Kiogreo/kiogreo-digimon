/** Metadata required by the OpenCode runtime. */
export const meta = {
  name: "saveMemorySkill",
  description:
    "Append a memory string to a per‑Digimon JSON file (creates file/folder if needed).",
  version: "0.1.0",
};
